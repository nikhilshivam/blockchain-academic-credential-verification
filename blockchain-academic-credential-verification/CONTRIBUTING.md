# Contributing

1. Fork the repository.
2. Create a feature branch.
3. Make focused changes.
4. Run `npm test`.
5. Run the frontend build with `npm run build` inside `client`.
6. Open a pull request with a clear description.

Please never commit private keys, `.env` files, student personal data, or production credentials.
